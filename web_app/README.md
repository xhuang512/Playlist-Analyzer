# Playlist Analyzer Web App

Run the local Flask app from the project root:

```bash
/opt/miniconda3/envs/cpsc330/bin/python web_app/app.py
```

Then open:

```text
http://127.0.0.1:5001
```

The app reads the current CSV exports from `notebooks/data/`.

